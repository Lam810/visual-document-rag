# Core application. Install the accelerator stack separately:
#   Ascend NPU -> requirements-npu.txt
#   NVIDIA GPU -> a CUDA build of torch from https://pytorch.org
flask>=3.0,<4.0
werkzeug>=3.0,<4.0

# LangChain is used only for text splitting, the Chroma wrapper and the
# Embeddings interface, so the split packages are pinned rather than the meta
# package.
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4
chromadb>=0.5,<0.7

modelscope>=1.15
pyyaml>=6.0
numpy>=1.24,<2.0
tqdm>=4.65
